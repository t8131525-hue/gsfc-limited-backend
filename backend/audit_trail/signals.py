# audit_trail/signals.py
# This file is intentionally left mostly empty to disable all CRUD signal handlers.
# All model-related logging is now handled by the AuditableMixin.
# We only keep the code for custom events like Login/Logout if needed elsewhere.
pass